{
  "name": "piko-raises-bridge",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types --strict-vars=false"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.125.0"
  }
}
